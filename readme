

// package.json 파일을 생성 새로운 프로젝트 , 패키지 생성
npm init

// dependencies 배포용, devDependencies 개발용 dependencies 를 나타냄
--save-dev

// express 기반 서버 설치  express 의존성 dependencies 주입
npm install express --save

// app.js 라는 파일이 실행파일일 경우
// 파일 실행
node app.js 

// js 파일 변경을 감지 서버프로세스 재시작하는 dependencies
npm install nodemon --save
nodemon app.js // -g 로 설치하여야 실행이 가능함

// npm 글로벌 다운로드 모든 경로에서 사용가능하도록 설치할때 : -g ex) npm install {} -g , 관리자 권한이 필요함 sudo 를 사용

// body-parser : post req 처리시
npm install body-parser --save

// passport, passport-local, express-session, connect-flash
// 메일인증, 로컬 디비 저장, 세션관련 처리, 에러메시지 전달관련 모듈
npm install passport passport-local express-session connect-flash  --save-dev

// git hub desktop 프로그램

// sourcetree git 프로그램

// git 사용법
// 신규 생성 , 기존 프로젝트 생성
git init , git clone

// git hub 프로젝트 생성후
// {정보} 는 git hub 에 있음
// 프로젝트 서버 연결
git remote {정보} 

// 프로젝트 연결되었는지 확인
git remote -v

// 파일 추가
git add {파일명}
git add --all // 모든 파일 추가

// 파일 커밋
git commit -m "{입력할 메세지}"

// 서버에 올리기
git push {저장소명} {브랜치명}
git push origin master

// .gitignore 파일에 저장되어있는 내용은 커밋되지 않음



